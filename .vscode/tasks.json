{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Flask App",
            "type": "shell",
            "command": "source venv/bin/activate && flask --app app run",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
